class Main {

    function void main() {
        var Array A;

        let A = Array.new(4);

        let A[0] = 1;
        let A[1] = 3;
        let A[2] = 5;
        let A[3] = 2;

        do QuickSort.sort(A, 0, 3);

        do QuickSort.printArray(A, 4);

        do A.dispose();

        return;
    }
}